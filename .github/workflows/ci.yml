name: CI Quality Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy lints
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run domain tests
        run: cargo test -p domain

  build-deploy:
    needs: quality-gate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: Install Trunk
        env:
          TRUNK_VERSION: v0.21.14
        run: |
          curl -sfL "https://github.com/trunk-rs/trunk/releases/download/${TRUNK_VERSION}/trunk-x86_64-unknown-linux-gnu.tar.gz" | tar xzf - -C /usr/local/bin
          trunk --version

      - name: Build WASM app
        run: trunk build --release --public-url /peach-web/

      - name: Copy index.html to 404.html for SPA routing
        run: cp dist/index.html dist/404.html

      - name: Inject build hash into service worker
        run: |
          HASH=$(sha256sum dist/*_bg.wasm | head -1 | cut -c1-16)
          if [ -z "$HASH" ]; then echo "::error::No *_bg.wasm found in dist/" && exit 1; fi
          sed -i "s/const CACHE_VERSION = 'peach-v1'/const CACHE_VERSION = 'peach-${HASH}'/" dist/sw.js
          grep -q "peach-${HASH}" dist/sw.js || { echo "::error::Service worker cache version injection failed — sw.js may have been reformatted"; exit 1; }
          echo "Injected cache version: peach-${HASH}"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
